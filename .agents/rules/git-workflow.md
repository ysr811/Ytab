# Git Workflow Rules

- **Never Push Automatically**: Do NOT run `git push` automatically or proactively after making changes, committing, or building.
- **Explicit Request Only**: Only execute `git push` when the user explicitly requests to push / upload to GitHub.
